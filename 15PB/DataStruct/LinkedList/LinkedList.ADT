ADT 链表（List）

Data

除第一个元素A1外，每一个元素有且只有一个直接前驱元素，除了最后一个元素
An外，每个元素有且只有一个后继元素，数据元素之间的关系是一对一的关系

Operation

1.初始化操作，建立一个空的链表
2.若链表为空，返回true，否则返回false
3.将链表清空
4.将链表中的第i个元素返回给e
5.在链表中查找与给定值e相等的元素，成功返回序号，否则返回0
6.在链表中的第i个位置中插入新元素
7.在链表中删除第i个位置的元素，并用e返回其值
8.返回链表L的元素个数

endADT
